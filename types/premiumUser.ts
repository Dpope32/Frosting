export interface PremiumUser {
    id: string;
    username: string;
    device_id: string;
    purchase_date: string;
    is_active: boolean;
    user_email: string;
    plan_id: string;
    ls_subscription_id: string;
    created: string;
    updated: string;
  }